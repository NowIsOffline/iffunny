import React from "react";

export const metadata = {
    description: "Choice Helper is a tool to help you make decisions quickly by selecting random options. Ideal for those with decision-making difficulties.",
    viewport: "width=device-width, initial-scale=1.0",
    charSet: "UTF-8",
    keywords: "decision maker, choice helper, random selection, decision making tool, choose easier,choice-helper,decision maker,web-choice-helper"
};

export default function PuzzleReaderLayout({children}) {
    return (
        <div>
        {children}
        </div>
    );
}
